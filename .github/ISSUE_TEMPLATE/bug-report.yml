name: Bug Report
description: Report a bug or issue with the application
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        ⚠️ **Important: This template is only for technical bugs (issues with the software).**
        
        For questions (Q&A) and feature requests (Ideas), please use [GitHub Discussions](https://github.com/s1t5/mail-archiver/discussions) instead.
        
        Thanks for taking the time to report this bug!
        Please fill out the form below to help us understand and fix the issue.

  - type: textarea
    id: description
    attributes:
      label: Bug Description
      description: Provide a clear and concise description of the bug. Include screenshots if necessary.
      placeholder: 'Example: "The export function fails when trying to export more than 1000 emails"'
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to Reproduce
      description: Describe the steps to reproduce the bug
      placeholder: |
        1. Go to '...'
        2. Click on '...'
        3. Scroll down to '...'
        4. See error
    validations:
      required: true

  - type: dropdown
    id: version
    attributes:
      label: Application Version
      description: Which version are you using?
      options:
        - "2607.1"
        - "2606.5"
        - "2606.4"
        - "2606.3"
        - "2606.2"
        - "2601.1"
        - "2605.2"
        - "2605.1"
        - "2604.2"
        - "2604.1"
        - "2603.1"
        - "2602.4"
        - "2602.3"
        - "2602.2"
        - "2602.1"
        - "older"
      default: 0
    validations:
      required: true

  - type: dropdown
    id: installation-type
    attributes:
      label: Installation Type
      description: How is the application installed?
      options:
        - "Docker"
        - "Standalone"
        - "Other"
      default: 0
    validations:
      required: false

  - type: input
    id: management-tools
    attributes:
      label: Management Tools
      description: Are Docker management tools in use?
      placeholder: 'e.g., Portainer, etc.'
    validations:
      required: false

  - type: input
    id: host-system
    attributes:
      label: Host System
      description: What is the host operating system?
      placeholder: 'e.g., Ubuntu 24.04, Debian 12, Windows Server 2022, etc.'
    validations:
      required: false

  - type: input
    id: mail-provider
    attributes:
      label: Mail Provider
      description: Which mail provider is being archived?
      placeholder: 'e.g., Mailbox.org, Gmail, etc.'
    validations:
      required: false

  - type: textarea
    id: logs
    attributes:
      label: Relevant Log Output
      description: |
        Please copy and paste relevant log output from `docker compose logs`.
        **Important: Ensure proper anonymization of sensitive data (email addresses, domains, credentials, etc.)!**
        Alternatively, you can submit logs via email if they contain sensitive information.
      render: shell
    validations:
      required: true
